@PACKAGE_INIT@

if (NOT TARGET imgui::imgui)
  include(${CMAKE_CURRENT_LIST_DIR}/@targets_export_name@.cmake)
endif ()

check_required_components(imgui)
set(imgui_INCLUDE_DIRS ${PACKAGE_PREFIX_DIR}/include)
